import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    text: {
        fontSize: 30,
        paddingBottom: 16,
        textAlign: 'left'
    },
    bookings: {
        fontSize: 20,
    }
});
